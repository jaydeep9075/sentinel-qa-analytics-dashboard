// lib/brand.ts
//
// How the product introduces itself. One name and one line, imported by the
// landing footer, the sign-in card and the registration card — three places
// that had drifted into three different names for the same thing.
//
// TR-Insight is the test-reporting dashboard in the TR-TestIntelligence
// suite (TR = Testrig) — alongside TR-Forge (test case generation) and
// TR-Automate (Playwright automation with AI). No other product name or
// mark belongs on this surface.

export const BRAND_NAME = "TR-Insight";

/** The one-liner shown under the name. Keep it to a single sentence. */
export const BRAND_TAGLINE = "AI QE that knows your results";
